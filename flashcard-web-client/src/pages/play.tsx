import styles from "@/styles/Play.module.css";
import { NextPage } from "next";


const Play: NextPage = () => {
    return (
        <main className={styles.main}>
            <h1>Play Page</h1>
        </main>
    );
};

export default Play;